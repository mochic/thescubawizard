import React from 'react'

import styled from 'styled-components'

// this is the transparent sheet that goes over the background
const PageContainer = styled.div`
  height: 60px;
  width: 60px;
  border-radius: 100%;

  &:after {
  }
`
